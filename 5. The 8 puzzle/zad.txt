0. Запишете си присъствието с парола: nb07kd

The 8-puzzle game:

1. Нарисувайте таблица 3 на 3. Във всяка плочка има цифра от 0 до 8:
- 0 обозначава празно
- 1,2,3,4,5,6,7,8 - обозначават непразна плочка
- Нека таблицата е с размери 600 на 600
context.fillText(TEXT, X, Y)

Ползвайте двумерни масиви за да направите игралното поле

2. Когато кликнем върху плочка - мърдаме я ако може.

3. Вместо цифри - нека се рисуват части от картини в плочките, така че
когато пъзела е нареден да се образува цяла картинка.

context.drawImage(back3d.img, 0, 0, 200, 200);
context.drawImage(back3d.img, 0, 0, 200, 200, mouseX, mouseY, 500, 500);

4. Пъзела е с размери N реда на N колони. N се избира от потребителя в началото на играта.

let A = prompt("KOLKO SI GROZEN")
console.log(A)

- Как да направим число от String в JavaScript?
String - символен низ - поредица от символи (текст в частен случай)


Домашна работа:
1. Плочите се разменят само ако са съседни
2. Нарисувайте изрязък от картинка за всяка плочка (освен празната)
3. Проверка дали полето е наредено - ако полето е наредено - печелим
4. Автоматично генериране на поле
(Не всички полета са разрешими)
5. В началото на играта потребителя въвежда число - N. Размерът на игралното поле е N реда на N колони.

- Направете функция/код, който създава едномерен масив с размер N
- направете функция/код, който преизползва кода за създаване на едномерен масив, за да създаде друмерен масив

function suzdai_ednomeren_masiv_s_razmer_N() {
    .....
    return едномерен_масив_размер_N;
}

let dvumeren_masiv = [];
for(let i = 0; i < N; i++) {
    dvumeren_masiv[i] = suzdai_ednomeren_masiv_s_razmer_N()
}